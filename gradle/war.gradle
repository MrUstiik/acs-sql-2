apply plugin: "war"

bootWar {
    mainClassName = "com.ftr.dgb.payments.action.catalog.ActionCatalogServiceSqlApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
